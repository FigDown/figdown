Line 44: illegal character U+0000 — XML forbids it, so the artifact could not be well-formed (below U+0020 only tab, newline and carriage return are allowed)
Line 45: illegal character U+0008 — XML forbids it, so the artifact could not be well-formed (below U+0020 only tab, newline and carriage return are allowed)
Line 46: illegal character U+000B — XML forbids it, so the artifact could not be well-formed (below U+0020 only tab, newline and carriage return are allowed)
Line 47: illegal character U+000C — XML forbids it, so the artifact could not be well-formed (below U+0020 only tab, newline and carriage return are allowed)
Line 48: illegal character U+0000 — XML forbids it, so the artifact could not be well-formed (below U+0020 only tab, newline and carriage return are allowed)
Line 49: illegal character U+0001 — XML forbids it, so the artifact could not be well-formed (below U+0020 only tab, newline and carriage return are allowed)
Line 49: illegal character U+000E — XML forbids it, so the artifact could not be well-formed (below U+0020 only tab, newline and carriage return are allowed)
Line 49: illegal character U+001B — XML forbids it, so the artifact could not be well-formed (below U+0020 only tab, newline and carriage return are allowed)
Line 49: illegal character U+001F — XML forbids it, so the artifact could not be well-formed (below U+0020 only tab, newline and carriage return are allowed)
Line 50: illegal character U+FFFE — XML forbids it, so the artifact could not be well-formed (U+FFFE, U+FFFF and unpaired surrogates are not characters — no escape writes one)
Line 50: illegal character U+FFFF — XML forbids it, so the artifact could not be well-formed (U+FFFE, U+FFFF and unpaired surrogates are not characters — no escape writes one)
