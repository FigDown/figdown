Line 20: "group" is not allowed in genre flowchart — it was WITHDRAWN from this genre, not misspelled: `flowchart` no longer declares `group`. Its one occurrence in the corpus was this genre's own reference figure, which exists to demonstrate every form of every keyword — so citing it as evidence of need is circular. There were no authored uses. Subject vocabulary is per genre (core §3, `GENRE-VOCABULARY-OBLIGATION`): a spelling accepted by several genres is several independent declarations, and this genre's was withdrawn without touching any other's. (withdrawn, `SCENE-KEYWORD-MEMBERSHIP`; MIGRATIONS 0.3)
Line 21: "threshold" is not allowed in genre flowchart — it was WITHDRAWN from this genre, not misspelled: `flowchart` no longer declares `threshold`. Zero occurrences, and the construct does not apply: a threshold is a labelled reference value drawn at a percentage of the target's RENDERED EXTENT, and a process box's extent is an artifact of its label length, so the line asserts nothing a reader can read. Subject vocabulary is per genre (core §3, `GENRE-VOCABULARY-OBLIGATION`): a spelling accepted by several genres is several independent declarations, and this genre's was withdrawn without touching any other's. (withdrawn, `SCENE-KEYWORD-MEMBERSHIP`; MIGRATIONS 0.3)
Line 22: "band" is not allowed in genre flowchart — it was WITHDRAWN from this genre, not misspelled: `flowchart` no longer declares `band`. Zero occurrences, and a band is a RANGE over that same meaningless extent. Subject vocabulary is per genre (core §3, `GENRE-VOCABULARY-OBLIGATION`): a spelling accepted by several genres is several independent declarations, and this genre's was withdrawn without touching any other's. (withdrawn, `SCENE-KEYWORD-MEMBERSHIP`; MIGRATIONS 0.3)
Line 23: "bundle" is not allowed in genre flowchart — it was WITHDRAWN from this genre, not misspelled: `flowchart` no longer declares `bundle`. Zero occurrences, and parallel flowlines between the same two stages are different CONDITIONS; drawing a ring round them hides what the figure is for. Subject vocabulary is per genre (core §3, `GENRE-VOCABULARY-OBLIGATION`): a spelling accepted by several genres is several independent declarations, and this genre's was withdrawn without touching any other's. (withdrawn, `SCENE-KEYWORD-MEMBERSHIP`; MIGRATIONS 0.3)
