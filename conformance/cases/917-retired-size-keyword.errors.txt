Line 10: size has been retired: its keys moved onto pin — write pin <id> width=<px> height=<px> (one directive carries an element's whole declared geometry: at= places it, width=/height= extend it; all three keys are optional and a pin with none of them declares nothing) (MIGRATIONS)
Line 15: size has been retired: its keys moved onto pin — write pin <id> width=<px> height=<px> (one directive carries an element's whole declared geometry: at= places it, width=/height= extend it; all three keys are optional and a pin with none of them declares nothing) (MIGRATIONS)
