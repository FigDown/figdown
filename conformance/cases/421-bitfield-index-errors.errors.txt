Line 10: index= takes a range written <first>..<last> — one ".." separator, and both ends present: index=0..7, index=53..0 (descending), index="0..Last Entry" when the last index is only named in prose, or index="" to say it repeats without stating any index
Line 11: index= needs a LITERAL first index, and "n" is not one: a FigDown number is a run of DIGITS — no sign, no decimal point, no separators. Only the LAST index may be prose — the first is machine-readable by construction, which is what lets a reader place the run without resolving any name. Write index=<digits>..<last>: index=0..7, or index="0..n" if the run really starts at 0 and ends where "n" says
Line 12: index= needs a LITERAL first index, and "-1" is not one: a FigDown number is a run of DIGITS — no sign, no decimal point, no separators. Only the LAST index may be prose — the first is machine-readable by construction, which is what lets a reader place the run without resolving any name. Write index=<digits>..<last>: index=0..7, or index="0..-1" if the run really starts at 0 and ends where "-1" says
Line 13: index= first index "9007199254740993" is above 9007199254740991, the largest integer a FigDown number carries exactly (core §12.5 binds a number to an ECMAScript double) — a reader would get 9007199254740992 back, not what you wrote
Line 14: index= last index "9007199254740993" is above 9007199254740991, the largest integer a FigDown number carries exactly (core §12.5 binds a number to an ECMAScript double) — a reader would get 9007199254740992 back, not what you wrote, and the element count with it
Line 15: index=3..3 is a range of ONE element, which is not repetition — drop index= (an absent key claims nothing), or write the range the run really covers
Line 16: index= takes a range written <first>..<last> — one ".." separator, and both ends present: index=0..7, index=53..0 (descending), index="0..Last Entry" when the last index is only named in prose, or index="" to say it repeats without stating any index
Line 17: duplicate option "index=" on one line
Line 18: index= does not apply to a * field: * means the length is NOT in the document, so "an element of unstated width, repeated" asserts nothing a reader could use. Give the repeated element its real width and put the range on it (field "Segment List" 128 index="0..Last Entry"), or keep * and drop index=
Line 19: index= is not available on the compact field form — the range would apply LINE-wide, saying that every item repeats over the same indices. Write the repeated element in the classic form on its own line: field "<name>" <width> index=0..7
Line 8: index= takes a range written <first>..<last> — one ".." separator, and both ends present: index=0..7, index=53..0 (descending), index="0..Last Entry" when the last index is only named in prose, or index="" to say it repeats without stating any index
Line 9: index= takes a range written <first>..<last> — one ".." separator, and both ends present: index=0..7, index=53..0 (descending), index="0..Last Entry" when the last index is only named in prose, or index="" to say it repeats without stating any index
