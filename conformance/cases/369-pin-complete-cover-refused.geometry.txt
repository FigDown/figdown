Line 18: pin puts "b" completely over "a" — a node drawn entirely inside another is invisible, so the figure states that "a" is not there. Move the pin off "a", or pin "a" too if the cover is deliberate — document order is paint order (`PAINT-ORDER-CONSTRUCT`), so two author-placed boxes may overlap however the author says. Partial overlap is not this error; only a complete cover is.
