Line 25: id= requires figdown 0.5 (this document declares 0.4): a connector has no id= spelling under figdown 0.4, so an engine held to the version this document declares — the archived 0.4 engine, or a reader working from the frozen read/0.4 contract — would refuse this line. A header that stops determining what the language accepts is a comment, not a declaration. Raise the header to figdown 0.5, or delete the key: an anonymous connector stays legal and claims nothing less (MIGRATIONS 0.5)
Line 26: id= requires figdown 0.5 (this document declares 0.4): a connector has no id= spelling under figdown 0.4, so an engine held to the version this document declares — the archived 0.4 engine, or a reader working from the frozen read/0.4 contract — would refuse this line. A header that stops determining what the language accepts is a comment, not a declaration. Raise the header to figdown 0.5, or delete the key: an anonymous connector stays legal and claims nothing less (MIGRATIONS 0.5)
Line 35: id= requires figdown 0.5 (this document declares 0.1): a connector has no id= spelling under figdown 0.1, so an engine held to the version this document declares — the archived 0.1 engine, or a reader working from the frozen read/0.1 contract — would refuse this line. A header that stops determining what the language accepts is a comment, not a declaration. Raise the header to figdown 0.5, or delete the key: an anonymous connector stays legal and claims nothing less (MIGRATIONS 0.5)
