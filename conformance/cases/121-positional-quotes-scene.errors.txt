Line 3: class meaning must be quoted: class hot "Hot" — whitespace also separates positionals, so a bare token cannot express a phrase (MIGRATIONS 0.1)
Line 4: node label must be quoted: node a "Cache" — whitespace also separates positionals, so a bare token cannot express a phrase (MIGRATIONS 0.1)
Line 6: group label must be quoted: group g "Store" — whitespace also separates positionals, so a bare token cannot express a phrase (MIGRATIONS 0.1)
Line 7: external label must be quoted: external e "External" — whitespace also separates positionals, so a bare token cannot express a phrase (MIGRATIONS 0.1)
