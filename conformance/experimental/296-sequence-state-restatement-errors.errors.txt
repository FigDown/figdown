Line 15: lifeline "c" is already in state "INIT" — a state that has not changed is never restated, and a transition is derived from the adjacent pair, so a restatement would assert a transition that did not happen (draft §23.2). Occurrences in DIFFERENT operands of one fragment are exclusive and do not restate each other; this pair is not that
Line 20: lifeline "c" is already in state "BOUND" — a state that has not changed is never restated, and a transition is derived from the adjacent pair, so a restatement would assert a transition that did not happen (draft §23.2). Occurrences in DIFFERENT operands of one fragment are exclusive and do not restate each other; this pair is not that
Line 22: lifeline "c" is already in state "REQUESTING" — a state that has not changed is never restated, and a transition is derived from the adjacent pair, so a restatement would assert a transition that did not happen (draft §23.2). Occurrences in DIFFERENT operands of one fragment are exclusive and do not restate each other; this pair is not that
