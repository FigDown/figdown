Line 10: "state" is not the word genre block uses for this — write "node": this genre has more kinds of thing than it has words for, so `node` is the general one. Each genre takes the term its own domain uses (block/topology `node` `edge`, flowchart `node` `flowline`, statechart `state` `transition`, sequence `lifeline` `message`) — run tools/migrate-figdown.js to rewrite it (MIGRATIONS 0.2)
Line 8: "flowline" is not the word genre block uses for this — write "edge": a block or topology figure is a graph, and `edge` is the graph word (DOT). Each genre takes the term its own domain uses (block/topology `node` `edge`, flowchart `node` `flowline`, statechart `state` `transition`, sequence `lifeline` `message`) — run tools/migrate-figdown.js to rewrite it (MIGRATIONS 0.2)
Line 9: "transition" is not the word genre block uses for this — write "edge": a block or topology figure is a graph, and `edge` is the graph word (DOT). Each genre takes the term its own domain uses (block/topology `node` `edge`, flowchart `node` `flowline`, statechart `state` `transition`, sequence `lifeline` `message`) — run tools/migrate-figdown.js to rewrite it (MIGRATIONS 0.2)
