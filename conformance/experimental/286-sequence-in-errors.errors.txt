Line 11: fragment "f" has no members — a fragment's extent is the span of the lines carrying in=f, and a container with no extent asserts nothing
Line 12: operand "o" has no members — an operand's extent is the span of the lines carrying in=o, and a container with no extent asserts nothing
Line 13: unknown fragment or operand "c" — in= on a message names the fragment or operand this message occurs inside, and "c" is a LIFELINE. A message already names its lifelines through its two endpoints; a state names its lifeline in slot 1. in= is containment, not participation. Under sequence in= is accepted on message, operand, lifeline, state and fragment — five acceptors, all sense 1 (UML 2.5.1 §17.12.13: a StateInvariant and a CombinedFragment are both InteractionFragments, so an InteractionOperand contains them both) — and its value is always a fragment or an operand id (draft §33.7)
Line 14: unknown fragment or operand "nowhere" — in= on a message names the fragment or operand this message occurs inside. Under sequence in= is accepted on message, operand, lifeline, state and fragment — five acceptors, all sense 1 (UML 2.5.1 §17.12.13: a StateInvariant and a CombinedFragment are both InteractionFragments, so an InteractionOperand contains them both) — and its value is always a fragment or an operand id (draft §33.7)
Line 15: unknown fragment or operand "c" — in= on a state names the fragment or operand this state occurs inside, and "c" is a LIFELINE. A message already names its lifelines through its two endpoints; a state names its lifeline in slot 1. in= is containment, not participation. Under sequence in= is accepted on message, operand, lifeline, state and fragment — five acceptors, all sense 1 (UML 2.5.1 §17.12.13: a StateInvariant and a CombinedFragment are both InteractionFragments, so an InteractionOperand contains them both) — and its value is always a fragment or an operand id (draft §33.7)
Line 16: operand "p" has no members — an operand's extent is the span of the lines carrying in=p, and a container with no extent asserts nothing
Line 16: unknown fragment "o" — operand in= names a FRAGMENT, not another operand: an operand is a compartment of a fragment, and a compartment has no compartments of its own (UML 2.5.1 §17.12.3: a CombinedFragment owns its operands)
Line 17: operand "q" has no members — an operand's extent is the span of the lines carrying in=q, and a container with no extent asserts nothing
Line 17: unknown fragment "c" — operand in= names a FRAGMENT, not a lifeline: an operand is a compartment of a fragment, and a fragment spans lifelines rather than belonging to one
Line 18: fragment "g" has no members — a fragment's extent is the span of the lines carrying in=g, and a container with no extent asserts nothing
Line 18: unknown fragment or operand "s" — in= on a fragment names the fragment or operand this fragment occurs inside, and "s" is a LIFELINE. A message already names its lifelines through its two endpoints; a state names its lifeline in slot 1. in= is containment, not participation. Under sequence in= is accepted on message, operand, lifeline, state and fragment — five acceptors, all sense 1 (UML 2.5.1 §17.12.13: a StateInvariant and a CombinedFragment are both InteractionFragments, so an InteractionOperand contains them both) — and its value is always a fragment or an operand id (draft §33.7)
