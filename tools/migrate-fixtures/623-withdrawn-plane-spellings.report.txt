  L6  [withdrawn-plane] `plane` is WITHDRAWN from the language (`PAINT-ORDER-CONSTRUCT`) — removed, not renamed, so there is NO replacement spelling and NO mechanical rewrite. DELETE the line and every plane= that referenced it. Paint order is document order: a later line paints on top, so the drawing is unchanged unless the planes were written out of document order — check the result. If the elements formed a logical layer of the SUBJECT (an overlay, a control plane), that meaning belongs in a class= whose LABEL states it (core §5, `PRESENTATION-AS-MEANING-CARRIER`), which is where the corpus was already carrying it. The freeze contract promises mechanical migration for FROZEN constructs; `plane` was EXPERIMENTAL, so none was owed. See spec/migrations.md 0.3 and design/scene-keyword-audit.md §4.2.  line: plane overlay "VXLAN tunnels" z-index=2
  L7  [withdrawn-plane] this line uses an option key WITHDRAWN from the language (`PAINT-ORDER-CONSTRUCT`) — removed, not renamed, so there is NO replacement spelling and NO mechanical rewrite. DELETE the line and every plane= that referenced it. Paint order is document order: a later line paints on top, so the drawing is unchanged unless the planes were written out of document order — check the result. If the elements formed a logical layer of the SUBJECT (an overlay, a control plane), that meaning belongs in a class= whose LABEL states it (core §5, `PRESENTATION-AS-MEANING-CARRIER`), which is where the corpus was already carrying it. The freeze contract promises mechanical migration for FROZEN constructs; `plane` was EXPERIMENTAL, so none was owed. See spec/migrations.md 0.3 and design/scene-keyword-audit.md §4.2.  line: edge a -> b plane=overlay
