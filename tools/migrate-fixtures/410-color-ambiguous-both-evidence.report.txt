  L1  [color-holds-file] 1 other mechanical rewrite(s) in this file were WITHHELD, not applied: boundary→external. An unresolved color= holds the whole file, because the refusals that classify it read EVIDENCE those rewrites would delete — migrate the older spellings first and --color-means has nothing left to refuse, so it would delete a FILL and report success. Settle color= (--color-means=fill|text, or by hand) and re-run; everything else then migrates in the same pass (MIGRATIONS)
  L6  [color-ambiguous] color= has two readings and nothing on this line chooses between them: it set the FILL of the box, or it set the colour of the LABEL — a channel v0.1 has no key for at all, so that reading DELETES the token. BOTH READINGS ARE RULED OUT BY THE FILE: this file also writes fill=, a key that did not exist while color= meant the FILL; and it writes boundary, a spelling already retired by the time color= meant the LABEL. No value of --color-means is accepted here — read these lines and rewrite them by hand. (MIGRATIONS)
