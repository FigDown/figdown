  L1  [color-holds-file] 1 other mechanical rewrite(s) in this file were WITHHELD, not applied: boundary→external. An unresolved color= holds the whole file, because the refusals that classify it read EVIDENCE those rewrites would delete — migrate the older spellings first and --color-means has nothing left to refuse, so it would delete a FILL and report success. Settle color= (--color-means=fill|text, or by hand) and re-run; everything else then migrates in the same pass (MIGRATIONS 0.1)
  L8  [color-ambiguous] color= has two readings and nothing on this line chooses between them: it set the FILL of the box, or it set the colour of the LABEL — a channel v0.1 has no key for at all, so that reading DELETES the token. ONE READING IS RULED OUT BY THE FILE: this file writes boundary, a spelling already retired by the time color= meant the LABEL — so this was a FILL. Re-run with --color-means=fill, which rewrites it to fill=; --color-means=text is REFUSED on this file. (MIGRATIONS 0.1)
