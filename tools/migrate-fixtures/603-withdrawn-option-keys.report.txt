  L5  [withdrawn-edge-geometry] this line uses an option key WITHDRAWN from the language (`EDGE-GEOMETRY-CONSTRUCTS`) — removed, not renamed, so there is NO replacement spelling and NO mechanical rewrite. DELETE the line and re-render: the edge will draw under auto layout, which is a DIFFERENT PICTURE, so check the result. `rank`, `flow`, declaration order and `pin` are the content-zone means of shaping it. The freeze contract promises mechanical migration for FROZEN constructs; these were EXPERIMENTAL, so none was owed. See spec/migrations.md 0.1 and spec/core.md §9 `EDGE-IDENTITY-AND-GEOMETRY`.  line: edge a -> b src=e dst=w
  L6  [withdrawn-edge-geometry] this line uses an option key WITHDRAWN from the language (`EDGE-GEOMETRY-CONSTRUCTS`) — removed, not renamed, so there is NO replacement spelling and NO mechanical rewrite. DELETE the line and re-render: the edge will draw under auto layout, which is a DIFFERENT PICTURE, so check the result. `rank`, `flow`, declaration order and `pin` are the content-zone means of shaping it. The freeze contract promises mechanical migration for FROZEN constructs; these were EXPERIMENTAL, so none was owed. See spec/migrations.md 0.1 and spec/core.md §9 `EDGE-IDENTITY-AND-GEOMETRY`.  line: edge a -> b via=40,10;80,10
  L7  [withdrawn-edge-geometry] this line uses an option key WITHDRAWN from the language (`EDGE-GEOMETRY-CONSTRUCTS`) — removed, not renamed, so there is NO replacement spelling and NO mechanical rewrite. DELETE the line and re-render: the edge will draw under auto layout, which is a DIFFERENT PICTURE, so check the result. `rank`, `flow`, declaration order and `pin` are the content-zone means of shaping it. The freeze contract promises mechanical migration for FROZEN constructs; these were EXPERIMENTAL, so none was owed. See spec/migrations.md 0.1 and spec/core.md §9 `EDGE-IDENTITY-AND-GEOMETRY`.  line: edge a -> b tailport=0.5 headport=0.5
  L8  [withdrawn-edge-geometry] this line uses an option key WITHDRAWN from the language (`EDGE-GEOMETRY-CONSTRUCTS`) — removed, not renamed, so there is NO replacement spelling and NO mechanical rewrite. DELETE the line and re-render: the edge will draw under auto layout, which is a DIFFERENT PICTURE, so check the result. `rank`, `flow`, declaration order and `pin` are the content-zone means of shaping it. The freeze contract promises mechanical migration for FROZEN constructs; these were EXPERIMENTAL, so none was owed. See spec/migrations.md 0.1 and spec/core.md §9 `EDGE-IDENTITY-AND-GEOMETRY`.  line: edge a -> b routing=straight
