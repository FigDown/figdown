  L5  [withdrawn-edge-geometry] `routing` is WITHDRAWN from the language (`EDGE-GEOMETRY-CONSTRUCTS`) — removed, not renamed, so there is NO replacement spelling and NO mechanical rewrite. DELETE the line and re-render: the edge will draw under auto layout, which is a DIFFERENT PICTURE, so check the result. `rank`, `flow`, declaration order and `pin` are the content-zone means of shaping it. The freeze contract promises mechanical migration for FROZEN constructs; these were EXPERIMENTAL, so none was owed. See spec/migrations.md 0.1 and spec/core.md §9 `EDGE-IDENTITY-AND-GEOMETRY`.  line: routing orthogonal
