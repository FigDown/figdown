  L2  [color-ambiguous] color= has two readings and nothing on this line chooses between them: it set the FILL of the box, or it set the colour of the LABEL — a channel v0.1 has no key for at all, so that reading DELETES the token. NOTHING IN THIS FILE RULES EITHER READING OUT — it writes no fill=, and none of the spellings that were retired before the LABEL reading existed — and the tool will not guess. The two readings differ in what was DRAWN, not in what is written: as a FILL the value painted the box interior, as a LABEL colour it painted only the text. Decide from the figure, then re-run with --color-means=fill or --color-means=text. (MIGRATIONS)
