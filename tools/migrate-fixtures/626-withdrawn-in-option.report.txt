  L7  [withdrawn-opt-from-genre] `in=` is no longer accepted by genre `flowchart` (`MEMBERSHIP-KEY-ACCEPTANCE`). Its only value domain was the id of a containing `group`, and this genre stopped declaring `group`, so every value was a dead end. There is NO mechanical rewrite: what expresses membership now is a `class` whose label names the partition, put on each member with `class=`. See spec/migrations.md 0.3.  line: process a "Parse" in=ingress
  L8  [withdrawn-opt-from-genre] `in=` is no longer accepted by genre `flowchart` (`MEMBERSHIP-KEY-ACCEPTANCE`). Its only value domain was the id of a containing `group`, and this genre stopped declaring `group`, so every value was a dead end. There is NO mechanical rewrite: what expresses membership now is a `class` whose label names the partition, put on each member with `class=`. See spec/migrations.md 0.3.  line: decision b "Match?" in=ingress
  L14  [withdrawn-opt-from-genre] `in=` is no longer accepted by genre `statechart` (`MEMBERSHIP-KEY-ACCEPTANCE`). Its only value domain was the id of a containing `group`, and this genre stopped declaring `group`, so every value was a dead end. There is NO mechanical rewrite: what expresses membership now is a `class` whose label names the partition, put on each member with `class=` — and note the spelling is RESERVED here for a `state`-id domain, should UML composite states be earned. See spec/migrations.md 0.3.  line: state s "IDLE" in=super
