BAD ROLE — `role: "picture"`, which is not one of the two closed
vocabulary members.

figdown-manifest.md §3.7 (`MANIFEST-ACCESSIBILITY-ROLE`) closes `accessibility.role` to exactly
`graphics-document` and `img` — the same two roles spec/figdown-a11y.md
§2.1-§2.2 admits, `ACCESSIBILITY-PROFILE`'s default and its one declared downgrade. `picture`
is a plausible-looking word that is neither: this field exists to carry a
publisher's declaration of an already-ruled choice, not to invent a third
role, and a schema that accepted a near-miss spelling would let an
undeclared, unrecognised role through silently.
is not one of
